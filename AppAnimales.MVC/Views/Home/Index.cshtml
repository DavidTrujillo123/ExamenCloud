@{
    ViewData["Title"] = "Home Page";
}

<style>
    body {
        font-family: Arial, sans-serif;
        color: white;
        overflow: hidden; /* Evita que los círculos se desborden */
        position: relative;
    }

    .titulo {
        background: rgba(0, 0, 0, 0.5);
        padding: 20px;
        margin-top: 100px;
        font-size: 40px;
        font-weight: bold;
        border-radius: 10px;
        color: white;
    }

    .micontenedor {
        padding: 20px;
        display: flex;
    }

    .tarjeta {
        background: #f7f7f7;
        color: #333;
        border-radius: 10px;
        padding: 20px;
        margin-bottom: 20px;
    }

        .tarjeta h2 {
            font-size: 24px;
            margin-bottom: 10px;
        }

        .tarjeta p {
            font-size: 16px;
        }

        .tarjeta img {
            max-width: 100%;
            border-radius: 5px;
            margin-bottom: 10px;
        }

    /* Estilos para los círculos */
    .circle {
        position: absolute;
        border-radius: 50%;
        width: 200px;
        height: 200px;
        filter: blur(50px); /* Aplicando el efecto de desenfoque */
    }
</style>

<div class="container text-center">
    <!-- Círculo 1 -->
    <div class="circle" style="background-color: rgba(255, 0, 0, 0.5);"></div>

    <!-- Círculo 2 -->
    <div class="circle" style="background-color: rgba(0, 255, 0, 0.5);"></div>

    <!-- Círculo 3 -->
    <div class="circle" style="background-color: rgba(0, 0, 255, 0.5);"></div>
    <div class="titulo">Bienvenido a mi MVC</div>
    <div class="micontenedor">
        <div class="tarjeta">
            @* <img src="https://upload.wikimedia.org/wikipedia/commons/7/7d/Microsoft_.NET_logo.svg" alt="Imagen de .NET"> *@
            <h2>¿Qué es .NET?</h2>
            <p>.NET es un framework de desarrollo de software de Microsoft que se utiliza para construir una variedad de aplicaciones, desde aplicaciones web hasta aplicaciones móviles y de escritorio.</p>
        </div>
        <div class="tarjeta">
            @* <img src="https://www.fixedbuffer.com/wp-content/uploads/2018/09/EFCore.png" alt="Imagen de Entity Framework"> *@
            <h2>¿Qué es Entity Framework?</h2>
            <p>Entity Framework es una parte de .NET que simplifica el acceso y la manipulación de datos en bases de datos relacionales, facilitando el desarrollo de aplicaciones con acceso a datos.</p>
        </div>
        <div class="tarjeta">
            @* <img src="https://i.blogs.es/ad7836/api/1366_2000.jpg" alt="Imagen de APIs"> *@
            <h2>¿Qué son las APIs?</h2>
            <p>Las APIs (Interfaz de Programación de Aplicaciones) permiten que diferentes aplicaciones se comuniquen entre sí, facilitando la integración de sistemas y el intercambio de datos.</p>
        </div>
    </div>

    
</div>

<script>
    // Mover los círculos de forma aleatoria
    var circles = document.querySelectorAll('.circle');
    circles.forEach(function(circle) {
        moveCircle(circle);
    });

    function moveCircle(circle) {
        var speedX = Math.random() * 2 + 1; // Velocidad en el eje X
        var speedY = Math.random() * 2 + 1; // Velocidad en el eje Y

        function move() {
            var x = circle.offsetLeft;
            var y = circle.offsetTop;

            // Cambiar la dirección si alcanza los bordes de la pantalla
            if (x <= 0 || x >= window.innerWidth - circle.offsetWidth) {
                speedX = -speedX;
            }
            if (y <= 0 || y >= window.innerHeight - circle.offsetHeight) {
                speedY = -speedY;
            }

            // Actualizar la posición
            circle.style.left = x + speedX + 'px';
            circle.style.top = y + speedY + 'px';

            // Repetir la animación
            requestAnimationFrame(move);
        }

        move();
    }
</script>
